<?php
/**
 * City Map block — view script module dependencies.
 *
 * Maintained manually (no build step required).
 * The @wordpress/interactivity module is resolved by the WordPress
 * importmap injected in <head>; Leaflet is loaded as a classic script.
 *
 * @package CityCore
 */
return array(
	'dependencies' => array(),
	'version'      => '0.1.0',
);
