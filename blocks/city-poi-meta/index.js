/**
 * City POI Meta — editor sidebar panels.
 *
 * DISABLED: POI meta fields (coordinates, quiz settings, reward image) are
 * edited exclusively via the classic metabox UI rendered by city-poi-meta.php.
 * The sidebar panels were removed to avoid duplication.
 *
 * @package CityCore
 */
